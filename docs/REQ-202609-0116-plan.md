# REQ-202609-0116：网页底部Bottom信息优化

## 需求背景

- **需求标题**：网页底部Bottom信息优化
- **需求编号**：REQ-202609-0116
- **优先级**：中
- **相关仓库**：`yokeay/baobeihuijia`

## 需求目标

网页底部的提示信息中，包含“SunChengxin 技术支持”相关内容。要求：
1. 删除“SunChengxin 技术支持”这段信息。
2. 保留“捐注服务器”和“捐注域名”的展示信息。
3. 注意处理多语言环境，确保其他语言文件中的对应技术支持信息也被同步删除。

## 执行计划

### 1. 准备工作
- 基于全局规范（CLAUDE.md），使用 git worktree 机制在仓库同级创建独立开发目录。
  ```bash
  git -C /root/baobeihuijia worktree add /root/baobeihuijia.wt-req-202609-0116 -b req-202609-0116
  ```
  *(已完成)*

### 2. 代码实现
- 切换到新建的 worktree 目录 `/root/baobeihuijia.wt-req-202609-0116`。
- 搜索代码库中涉及“技术支持”和“SunChengxin”的内容，尤其是底部组件（如 Footer 等）以及相关的国际化语言文件（如 `locales`, `i18n` 等配置或 json、yml 文件）。
- 按照需求，将涉及的文案删除，确保其他保留项（如捐赠服务器、捐赠域名）不受影响。
- 验证项目是否正常运行并检查底部信息是否修改成功（本地构建/Lint等）。

### 3. 代码提交与合入
- 将改动在需求分支 `req-202609-0116` 提交并推送到远端仓库。
- 根据 MEMORY.md 中的线索，该项目的生产/部署分支可能为 `local-server`，将需求分支合入到 `local-server` 中并推送到远端触发构建与部署（如果有需要，执行系统级的 build/restart 验证）。

### 4. 收尾与回调
- 检查提交和部署状态无误后，删除临时的 git worktree。
  ```bash
  git -C /root/baobeihuijia worktree remove /root/baobeihuijia.wt-req-202609-0116
  git -C /root/baobeihuijia worktree prune
  ```
- 最后执行回调命令，提交任务执行结果，任务状态标记完成。

```bash
curl -sS -X POST 'https://baseinfo.eu.cc/api/bus/cli-execution-callback' \
  -H 'Content-Type: application/json' \
  -H 'X-Bacs-Callback-Token: f443d00608ca61479870a458c3db398ff9bc2f5e67cc1ba3' \
  -d '{"taskId":"656a9a24-667b-4290-b849-1173b0041482","status":"done","summary":"移除多语言环境网页底部关于 SunChengxin 技术支持的信息"}'
```
